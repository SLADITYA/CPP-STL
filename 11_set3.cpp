// Unordered Set - Elements are stored in a random order. Doesn't hold duplicates.

// All the operations like insert, erase etc takes place in O(1) but in the worst case it is O(n)

#include<bits/stdc++.h>
using namespace std;

int main()
{
    // all the operations are similar to a set except lower_bound() and upper_bound() which doesn't work

    // has a better time complexity than a set in most of the cases, except some when collision happens

    unordered_set <int> st;
}